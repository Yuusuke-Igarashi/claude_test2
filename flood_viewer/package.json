{
  "name": "flood-viewer",
  "version": "0.3.0",
  "private": true,
  "description": "Standalone map viewer for the Tokyo 2024-08-21 flood candidate analysis (traffic + trajectory modes)",
  "type": "module",
  "scripts": {
    "data": "python3 sample/make_synthetic.py && python3 sample/make_trajectories.py",
    "build": "node build_standalone.mjs",
    "test": "node --test tests/*.test.js",
    "all": "npm run data && npm run build && npm test"
  },
  "devDependencies": {
    "maplibre-gl": "5.24.0",
    "playwright": "1.56.1"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
